<Window x:Class="VoiceLite.SettingsWindowNew"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:VoiceLite.Controls"
        mc:Ignorable="d"
        Title="VoiceLite Settings" Height="650" Width="850"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="500" MinWidth="700"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FAFAFA" Padding="20,15">
            <Grid>
                <TextBlock Text="VoiceLite Settings" FontSize="20" FontWeight="SemiBold" Foreground="#333333" HorizontalAlignment="Left"/>
                <TextBlock Name="VersionText" Text="" FontSize="12" Foreground="#95A5A6" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- License Status Banner -->
        <Border Grid.Row="1" Name="LicenseBanner" Padding="15,10" Background="#E8F5E9">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Name="LicenseBannerIcon" Text="✓" FontSize="16" FontWeight="Bold" Foreground="#4CAF50" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Grid.Column="1" Name="LicenseBannerText" Text="Pro License Active" FontSize="14" FontWeight="SemiBold" Foreground="#2E7D32" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="2" Name="LicenseBannerUpgrade" FontSize="12" VerticalAlignment="Center" Visibility="Collapsed">
                    <Hyperlink NavigateUri="https://voicelite.app" RequestNavigate="Hyperlink_RequestNavigate" Foreground="#1976D2">
                        <Run Text="Upgrade to Pro →"/>
                    </Hyperlink>
                </TextBlock>
            </Grid>
        </Border>

        <!-- Tabbed Content -->
        <TabControl Grid.Row="2"
                    Name="SettingsTabControl"
                    TabStripPlacement="Top"
                    Margin="0"
                    BorderThickness="0"
                    Background="White">
            <TabControl.Resources>
                <Style TargetType="TabItem" BasedOn="{StaticResource ModernSettingsTabStyle}"/>
            </TabControl.Resources>

            <!-- General Settings Tab -->
            <TabItem Header="General">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
                    <StackPanel>
                        <!-- Hotkey Settings -->
                        <GroupBox Header="Hotkey Settings" Margin="0,0,0,15">
                            <StackPanel Margin="10">
                                <TextBlock Text="Record Hotkey:" Margin="0,0,0,5"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" MaxWidth="300"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBox Name="HotkeyTextBox"
                                             Grid.Column="0"
                                             IsReadOnly="True"
                                             Text="F1"
                                             Padding="5"
                                             Background="#F0F0F0"
                                             PreviewKeyDown="HotkeyTextBox_PreviewKeyDown"
                                             GotFocus="HotkeyTextBox_GotFocus"
                                             LostFocus="HotkeyTextBox_LostFocus"/>

                                    <Button Name="ClearHotkeyButton"
                                            Grid.Column="1"
                                            Content="Clear"
                                            Width="60"
                                            Margin="5,0,0,0"
                                            Click="ClearHotkeyButton_Click"/>
                                </Grid>
                                <TextBlock Name="HotkeyInstructionText"
                                           Text="Click the box and press your desired key combination"
                                           FontSize="11"
                                           Foreground="Gray"
                                           Margin="0,5,0,0"
                                           Visibility="Visible"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- Recording Mode -->
                        <GroupBox Header="Recording Mode" Margin="0,0,0,15">
                            <StackPanel Margin="10">
                                <RadioButton Name="PushToTalkRadio"
                                             Content="Push-to-Talk (Hold key to record)"
                                             IsChecked="True"
                                             Margin="0,0,0,5"/>
                                <RadioButton Name="ToggleRadio"
                                             Content="Toggle (Press once to start, again to stop)"
                                             IsEnabled="True"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- Audio Settings -->
                        <GroupBox Header="Audio Settings" Margin="0,0,0,15">
                            <StackPanel Margin="10">
                                <TextBlock Text="Microphone:" Margin="0,0,0,5"/>
                                <ComboBox Name="MicrophoneComboBox"
                                          Margin="0,0,0,10"
                                          DisplayMemberPath="Name"
                                          MaxWidth="400"
                                          HorizontalAlignment="Left"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <Button Name="RefreshMicrophonesButton"
                                            Content="Refresh Microphones"
                                            Width="150"
                                            HorizontalAlignment="Left"
                                            Click="RefreshMicrophonesButton_Click"
                                            Margin="0,0,8,0"/>
                                    <Button Name="TestMicrophoneButton"
                                            Content="Test Microphone"
                                            Width="130"
                                            HorizontalAlignment="Left"
                                            Click="TestMicrophoneButton_Click"
                                            ToolTip="Click to start/stop recording test"/>
                                </StackPanel>

                                <CheckBox Name="AutoPasteCheckBox"
                                          Content="Auto-paste transcribed text (Ctrl+V)"
                                          IsChecked="True"
                                          Margin="0,0,0,0"
                                          ToolTip="When enabled, transcribed text will automatically paste into the active application. When disabled, text is only copied to clipboard."/>
                            </StackPanel>
                        </GroupBox>

                        <!-- Other Settings -->
                        <GroupBox Header="System Settings" Margin="0,0,0,15">
                            <StackPanel Margin="10">
                                <!-- Start with Windows feature - not yet implemented -->
                                <!--
                                <CheckBox Name="StartWithWindowsCheckBox"
                                          Content="Start with Windows"
                                          Margin="0,0,0,5"
                                          IsEnabled="False"/>
                                -->
                                <CheckBox Name="MinimizeToTrayCheckBox"
                                          Content="Minimize to system tray on close"
                                          IsChecked="True"
                                          Margin="0,0,0,0"/>
                                <!-- Show tray icon checkbox - always enabled, hidden since it's required -->
                                <!--
                                <CheckBox Name="ShowTrayIconCheckBox"
                                          Content="Show system tray icon"
                                          IsChecked="True"
                                          IsEnabled="False"
                                          Margin="0,0,0,5"/>
                                -->
                            </StackPanel>
                        </GroupBox>

                        <!-- License Status (Read-Only) -->
                        <GroupBox Header="License Status" Margin="0,0,0,15">
                            <StackPanel Margin="10">
                                <TextBlock Name="LicenseStatusText"
                                           Text="Loading license status..."
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,10"/>

                                <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="Gray" Margin="0,0,0,10">
                                    <Run Text="License is activated on first launch. To activate Pro or switch versions, restart the app."/>
                                </TextBlock>

                                <TextBlock FontSize="11" Foreground="Gray">
                                    <Run Text="Don't have a Pro license?"/>
                                    <Hyperlink NavigateUri="https://voicelite.app" RequestNavigate="Hyperlink_RequestNavigate">
                                        <Run Text="Get Pro for $20"/>
                                    </Hyperlink>
                                </TextBlock>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- AI Models Tab (Pro Only) -->
            <TabItem Header="AI Models" Name="ModelsTab">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
                    <StackPanel>
                        <!-- Info Banner -->
                        <Border Background="#E3F2FD"
                                CornerRadius="4"
                                Padding="15"
                                Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="📊 AI Model Selection"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="#1976D2"
                                           Margin="0,0,0,5"/>
                                <TextBlock TextWrapping="Wrap"
                                           FontSize="12"
                                           Foreground="#1976D2">
                                    Choose the AI model that best fits your needs. Larger models provide better accuracy but require more RAM and processing time. Tiny model comes pre-installed - other models can be downloaded on demand.
                                </TextBlock>
                            </StackPanel>
                        </Border>

                        <!-- Model Comparison & Download Control -->
                        <controls:ModelComparisonControl x:Name="ModelComparisonControl"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <!-- Footer with Buttons -->
        <Border Grid.Row="3"
                Background="#F5F5F5"
                BorderBrush="#E0E0E0"
                BorderThickness="0,1,0,0">
            <Grid Margin="20,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Status Text and Keyboard Shortcuts Hint -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Name="StatusText"
                               Text=""
                               FontSize="12"
                               Foreground="#606060"
                               VerticalAlignment="Center"
                               Margin="0,0,15,0"/>
                    <TextBlock Text="💡 Tip: Ctrl+S to save, Esc to cancel"
                               FontSize="11"
                               Foreground="#95A5A6"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button Name="ApplyButton"
                            Content="Apply"
                            Width="80"
                            Margin="0,0,10,0"
                            Click="ApplyButton_Click"/>
                    <Button Name="SaveButton"
                            Content="Save"
                            Width="80"
                            Margin="0,0,10,0"
                            Click="SaveButton_Click"
                            IsDefault="True"/>
                    <Button Name="CancelButton"
                            Content="Cancel"
                            Width="80"
                            Click="CancelButton_Click"
                            IsCancel="True"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
