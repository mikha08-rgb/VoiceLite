<Window x:Class="VoiceLite.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VoiceLite Sign In"
        Height="300"
        Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Sign in to VoiceLite"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16" />

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="Email" FontWeight="SemiBold" Margin="0,0,0,6" />
            <TextBox x:Name="EmailTextBox"
                     MinHeight="28"
                     Margin="0,0,0,6"
                     ToolTip="you@example.com" />
            <Button x:Name="SendLinkButton"
                    Content="Email me a magic link"
                    Height="32"
                    Margin="0,0,0,6"
                    Click="SendLinkButton_Click" />
            <TextBlock x:Name="StatusLabel"
                       FontSize="12"
                       Foreground="SteelBlue"
                       TextWrapping="Wrap" />
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="One-time code" FontWeight="SemiBold" Margin="0,0,0,6" />
            <TextBox x:Name="OtpTextBox"
                     MinHeight="28"
                     Margin="0,0,0,6"
                     MaxLength="6" />
            <Button x:Name="VerifyButton"
                    Content="Verify code"
                    Height="32"
                    Click="VerifyButton_Click" />
        </StackPanel>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    Margin="0,16,0,0">
            <Button Content="Close"
                    Width="80"
                    Margin="0,0,8,0"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
