<Window x:Class="VoiceLite.LicenseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="VoiceLite License" Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/ModernStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="#1e1e1e">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2d2d30" Padding="20">
            <StackPanel>
                <TextBlock Text="License Management"
                          FontSize="20" FontWeight="Bold"
                          Foreground="White"/>
                <TextBlock x:Name="LicenseStatusText"
                          Text="License Status"
                          FontSize="14"
                          Foreground="#7ACC7A"
                          Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <!-- Current License Info -->
                <Border Background="#2d2d30" CornerRadius="5" Padding="15" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Current License" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Type:" Foreground="#b4b4b4"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" x:Name="LicenseTypeText" Foreground="White"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Status:" Foreground="#b4b4b4" Margin="0,5,0,0"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" x:Name="StatusText" Foreground="White" Margin="0,5,0,0"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Expires:" Foreground="#b4b4b4" Margin="0,5,0,0"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" x:Name="ExpiryText" Foreground="White" Margin="0,5,0,0"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Features:" Foreground="#b4b4b4" Margin="0,5,0,0"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" x:Name="FeaturesText"
                                      Foreground="White" Margin="0,5,0,0" TextWrapping="Wrap"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Activation Section -->
                <Border Background="#2d2d30" CornerRadius="5" Padding="15">
                    <StackPanel>
                        <TextBlock Text="Activate License" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>

                        <TextBlock Text="License Key:" Foreground="#b4b4b4" Margin="0,0,0,5"/>
                        <TextBox x:Name="LicenseKeyTextBox"
                                Style="{StaticResource ModernTextBoxStyle}"
                                CharacterCasing="Upper"
                                MaxLength="19"/>

                        <TextBlock Text="Email:" Foreground="#b4b4b4" Margin="0,10,0,5"/>
                        <TextBox x:Name="EmailTextBox"
                                Style="{StaticResource ModernTextBoxStyle}"/>

                        <Button x:Name="ActivateButton"
                               Content="Activate License"
                               Style="{StaticResource ModernButtonStyle}"
                               Margin="0,15,0,0"
                               Click="ActivateButton_Click"/>

                        <TextBlock x:Name="ErrorText"
                                  Foreground="#ff6b6b"
                                  Margin="0,10,0,0"
                                  TextWrapping="Wrap"
                                  Visibility="Collapsed"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#2d2d30" Padding="15">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button x:Name="PurchaseButton"
                           Content="Purchase License"
                           Style="{StaticResource ModernButtonStyle}"
                           Margin="0,0,10,0"
                           Click="PurchaseButton_Click"/>
                    <Button x:Name="TrialButton"
                           Content="Continue Trial"
                           Style="{StaticResource ModernButtonStyle}"
                           Click="TrialButton_Click"/>
                </StackPanel>
                <Button x:Name="CloseButton"
                       Content="Close"
                       Style="{StaticResource ModernButtonStyle}"
                       HorizontalAlignment="Right"
                       Click="CloseButton_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>