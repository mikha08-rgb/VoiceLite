<Window x:Class="VoiceLite.SimpleLicenseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VoiceLite License" Height="350" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/ModernStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="VoiceLite License Manager"
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Status -->
        <Border Grid.Row="1" Background="#F0F0F0" Padding="10" CornerRadius="5" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Current Status:" FontWeight="Bold"/>
                <TextBlock x:Name="StatusText" Text="Checking..." Margin="0,5,0,0"/>
                <TextBlock x:Name="DetailsText" Text="" Margin="0,5,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- Activation -->
        <StackPanel Grid.Row="2" Margin="0,0,0,20">
            <TextBlock Text="Enter License Key:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="LicenseKeyBox"
                     Height="25"
                     CharacterCasing="Upper"
                     MaxLength="24"/>
            <TextBlock x:Name="ErrorText"
                       Foreground="Red"
                       Margin="0,5,0,0"
                       TextWrapping="Wrap"
                       Visibility="Collapsed"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="ActivateBtn" Content="Activate" Width="80" Height="25" Margin="0,0,10,0" Click="ActivateBtn_Click"/>
            <Button x:Name="BuyBtn" Content="Buy License" Width="80" Height="25" Margin="0,0,10,0" Click="BuyBtn_Click"/>
            <Button x:Name="CloseBtn" Content="Close" Width="80" Height="25" Click="CloseBtn_Click"/>
        </StackPanel>
    </Grid>
</Window>